import { default as React } from 'react';
import { eventWithTime } from '@rrweb/types';

interface ReplaySectionProps {
    events: eventWithTime[];
}
export declare const ReplaySection: React.FC<ReplaySectionProps>;
export {};
